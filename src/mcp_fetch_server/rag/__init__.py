"""Offline corpus, retrieval and local-model support.

Everything in this package is optional: the server runs without it as a plain
web fetch server. Installing the ``rag`` extra and setting ``FETCH_NET_MODE``
to ``offline`` or ``hybrid`` turns the same MCP tools into a browsable,
searchable local document archive.
"""
